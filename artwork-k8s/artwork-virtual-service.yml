apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: artwork-virtualservice
  namespace: default
spec:
  hosts:
    - artwork
  http:
    - route:
        - destination:
            host: artwork
            subset: "v1"
          weight: 100
      retries:
        attempts: 3
        perTryTimeout: 2s
